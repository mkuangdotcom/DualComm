# WhatsApp LLM Bridge

## Requirements

- Node 18+ (required for Telegram media downloads via native `fetch`)

## Telegram (optional)

Set the following environment variables to enable Telegram:

- `TELEGRAM_ENABLED=true`
- `TELEGRAM_BOT_TOKEN=...`
- `TELEGRAM_MEDIA_STAGING_DIR=media_staging/telegram`
